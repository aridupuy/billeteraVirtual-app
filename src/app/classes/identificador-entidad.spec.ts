import { IdentificadorEntidad } from './identificador-entidad';

describe('IdentificadorEntidad', () => {
  it('should create an instance', () => {
    expect(new IdentificadorEntidad()).toBeTruthy();
  });
});
